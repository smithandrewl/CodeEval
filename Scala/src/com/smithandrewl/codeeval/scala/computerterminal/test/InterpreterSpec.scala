package com.smithandrewl.codeeval.scala.computerterminal.test

import org.scalatest.path

class InterpreterSpec extends path.FunSpec {
  describe("The Interpreter") {
    describe("In Overwrite Mode") {

      it("Overwrites With Text") {
        fail()
      }

      it("Overwrites With Circumflex") {
        fail()
      }
    }

    describe("In Insert Mode") {
      it("Inserts With Text") {
        fail()
      }

    }

    describe("In Either Mode") {
      it("Clears The Screen") {
        fail()
      }

      it("Moves The Cursor") {
        fail()
      }

      it("Erases Characters To The Right") {
        fail()
      }

      it("Moves To An Arbitrary Location") {
        fail()
      }
    }
  }
}
